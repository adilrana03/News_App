/** @type {import('next').NextConfig} */
const nextConfig = {
      images: {
            domains: ['www.hindustantimes.com', 'timesofindia.indiatimes.com', 'images.moneycontrol.com']
      }
}

module.exports = nextConfig
